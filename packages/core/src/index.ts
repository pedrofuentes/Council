/**
 * @council-ai/core — placeholder entry point.
 *
 * This package reserves the `@council-ai/core` name in the workspace. The
 * shared deliberation engine, domain types, and memory layer will be extracted
 * here from `@council-ai/cli` in a future increment (see ROADMAP / DECISIONS).
 * Until then it is `private` and publishes nothing.
 */
export {};
