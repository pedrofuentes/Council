# @council-ai/core

> **Placeholder package — not yet published.**

Reserves the `@council-ai/core` name within the Council monorepo. The shared
deliberation engine, domain model, and memory layer currently live in
[`@council-ai/cli`](../cli) and will be extracted into this package in a future
increment so they can be reused by the CLI, a web app, and other surfaces.

This package is `private` and intentionally exports nothing today.
