# councilai

> **This package is a name placeholder.** The Council CLI is published as
> **[`@council-ai/cli`](https://www.npmjs.com/package/@council-ai/cli)**.

## Install the Council CLI

```bash
npm install -g @council-ai/cli
```

Then run:

```bash
council --help
```

## What is Council?

Council convenes a panel of AI experts that debate a question from multiple
viewpoints, genuinely disagree, remember past deliberations, and produce a
structured synthesis. It runs on your **GitHub Copilot** subscription — no API
keys, no separate accounts.

- **CLI package**: <https://www.npmjs.com/package/@council-ai/cli>
- **Repository**: <https://github.com/pedrofuentes/Council>

---

This `councilai` package (the no-hyphen spelling) exists only to reserve the name
and direct people to the canonical
[`@council-ai/cli`](https://www.npmjs.com/package/@council-ai/cli). It ships no
code, is intentionally non-functional, and is deprecated on npm.
